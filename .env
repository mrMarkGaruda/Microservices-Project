# Database configuration
# Use 'localhost' for local dev, 'postgres' for Docker Compose
DATABASE_URL=postgresql://fitness_user:fitness_password@postgres:5432/fitness_db

# Coach service configuration (updated ports)
# Use 'localhost' for local dev, 'coach' for Docker Compose
COACH_SERVICE_URL=http://coach:5001
USE_COACH_MICROSERVICE=true

# Flask configuration
FLASK_ENV=development
SECRET_KEY=fit-secret-key

# Bootstrap configuration
BOOTSTRAP_SECRET_KEY=bootstrap-secret-key

# API Configuration
API_KEY=some_very_secret_key