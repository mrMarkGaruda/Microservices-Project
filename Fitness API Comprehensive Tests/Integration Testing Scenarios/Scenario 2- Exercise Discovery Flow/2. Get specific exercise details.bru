meta {
  name: 2. Get specific exercise details
  type: http
  seq: 2
}

get {
  url: {{coach_service_base_url}}/exercises/{{valid_exercise_id}}
  body: none
  auth: none
}

tests {
  test("Status code is 200", function () {
      expect(res.getStatus()).to.equal(200);
  });
  expect(res.getBody()).to.have.property('id', bru.getEnvVar("valid_exercise_id"));
}
