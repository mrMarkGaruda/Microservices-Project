meta {
  name: 4. Get Single Exercise - Non-integer ID ('abc')
  type: http
  seq: 8
}

get {
  url: {{coach_service_base_url}}/exercises/abc
  body: none
  auth: none
}

tests {
  test("Status code is 400 or 404", function () {
  //     pm.response.to.be.oneOf([400, 404]);
  });
  
  test("Response contains an error message", function () {
      expect(res.getBody()).to.have.property('error');
  });
}
