meta {
  name: 0. Bootstrap Admin User (Run First)
  type: http
  seq: 1
}

post {
  url: {{monolith_service_base_url}}/bootstrap/admin
  body: json
  auth: none
}

headers {
  X-Bootstrap-Key: {{bootstrap_key}}
}

body:json {
  {
      "email": "{{admin_user_email}}",
      "password": "{{admin_user_password}}"
  }
}

tests {
  test("Status code is 200 or 201", function () {
  //     pm.response.to.be.oneOf([200, 201]);
  });
  test("Response indicates success", function () {
      const response = res.getBody();
      expect(response).to.have.property('success', true);
  });
}
