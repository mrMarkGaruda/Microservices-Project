meta {
  name: 4. Register Workout - Invalid Exercise IDs
  type: http
  seq: 21
}

post {
  url: {{monolith_service_base_url}}/workouts/register
  body: json
  auth: none
}

headers {
  X-API-Key: {{api_key}}
}

body:json {
  {
    "email": "{{test_user_email}}",
    "exercises": [1, 999, 1000]
  }
}

tests {
  test("Status code is 400", function () {
      expect(res.getStatus()).to.equal(400);
  });
  
  test("Response contains an error message", function () {
      expect(res.getBody()).to.have.property('error');
  });
}
